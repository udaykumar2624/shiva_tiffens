# Shiva Tiffin Center - Vite React Starter

This is a minimal runnable Vite + React project created from the PDF you provided.
It includes:
- Vite entry files
- Tailwind CSS wiring
- Simple UI primitives to satisfy imports
- Mock `MenuItem.list()` so the Menu page works without a backend

Run:
1. npm install
2. npm run dev
